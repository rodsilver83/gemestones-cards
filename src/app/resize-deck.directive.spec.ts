import { ResizeDeckDirective } from './resize-deck.directive';

describe('ResizeDeckDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeDeckDirective();
    expect(directive).toBeTruthy();
  });
});
